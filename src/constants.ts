
export enum operationTypes {
    expense = 'expense',
    income = 'income'
}

export const cellInputType = {
    date: 'date',
    amount: 'number',
    type: 'select',
    note: 'string',
}

export const localStorageTableKey = "myTable"

export const dateFormat = 'DD/MM/YYYY'